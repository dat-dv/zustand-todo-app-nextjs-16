
# --- Cấu hình Port cho Docker ---
PORT=3000

# PUBLIC ENV
NEXT_PUBLIC_SITE_URL=http://localhost:3000 # when deploy should match with expose port
NEXT_PUBLIC_API_URL=/api
NEXT_PUBLIC_IS_DEBUG=false

# PRIVATE ENV
NODE_ENV=development
JWT_SECRET=your_secret_key_here
SQLITE_DB_PATH=./data/sqlite.db
